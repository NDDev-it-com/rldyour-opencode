name: Dependency Review

on:
  pull_request:
    paths:
      - ".opencode/package.json"
      - ".opencode/package-lock.json"
      - ".github/workflows/**"
      - "scripts/**.py"

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: dependency-review-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - name: Review dependencies
        if: ${{ !github.event.repository.private }}
        uses: actions/dependency-review-action@4081bf99e2866ebe428fc0477b69eb4fcda7220a  # v4.5.0
        with:
          fail-on-severity: moderate
          comment-summary-in-pr: on-failure
      - name: Dependency review unavailable on private repositories
        if: ${{ github.event.repository.private }}
        run: |
          echo "::notice::GitHub Dependency Review requires Dependency Graph plus GitHub Advanced Security on private repositories."
          echo "::notice::Skipping dependency-review-action here; dependency-check.yml remains the portable pin/freshness gate."
